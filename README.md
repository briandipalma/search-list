# ES2015+ Babel, webpack and React search-list

# Setup

* Install node.
* Clone repo `git clone git@github.com:briandipalma/search-list.git`
* Run `npm i`
* Run `npm start`
* Navigate to http://localhost:8080/bundle
